@model IngresoUpdateDeleteViewModel
@{
    ViewData["Title"] = "Borrar Ingreso";
}

<h3 class"mb-3">¿Está seguro que desea borrar el ingreso: @Model.Nombre?</h3>

<form asp-action="DeleteIngresoDef">
    <input type="hidden" asp-for="IdIngreso" />
    <input type="hidden" asp-for="FechaIndex" />
    <button type="submit" class="btn btn-danger">Borrar</button>
    <a class="btn btn-secondary" asp-action="Index">Cancelar</a>
</form>
